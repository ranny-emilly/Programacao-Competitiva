#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define el "\n";

int main(){

    vector<vector<int>> m, n;

    for(int i = 0; i < 9; i++){
        for(int j = 0; j < 9; j++){
            cin >> m[i][j];
        }
    }
    for(int i = 0; i < 9; i++){
        for(int j = 0; j < 9; j++){
        vector <int> visto;
        find(visto.begin, visto.end, )
        
    }


    return 0;
}
